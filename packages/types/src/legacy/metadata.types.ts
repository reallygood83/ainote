export interface Metadata {
  username: string
  onboarded: boolean
  onboarded_tt: boolean
  dev_mode: boolean
  insider_mode: boolean
  horizon_limit: number
}

export interface LinkMetadata {
  title: string
  description: string
  image: string
  favicon: string
  author: string
  language: string
}
